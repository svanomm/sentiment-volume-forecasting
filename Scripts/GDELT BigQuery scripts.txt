SELECT * FROM `gdelt-bq.gdeltv2.gkg_partitioned` 
WHERE 
  TIMESTAMP_TRUNC(_PARTITIONTIME, DAY) >= TIMESTAMP("2018-01-01") 
  and (
	V2Organizations like '%American Airlines%'
     or V2Organizations like '%%'
     or V2Organizations like '%%'
     or V2Organizations like '%%'
     or V2Organizations like '%%'
     or V2Organizations like '%%'
     or V2Organizations like '%%'
	)
